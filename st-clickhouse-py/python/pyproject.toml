# No [project] metadata here on purpose: the package metadata lives in
# ../pyproject.toml (mirroring ../Cargo.toml). This file only carries
# tool configuration for the python/ sources and does not ship in the wheel.

[tool.pyrefly]
project-includes = [
    "**/*.py*",
    "**/*.ipynb",
]
