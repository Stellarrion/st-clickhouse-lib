[project]
name = "st-clickhouse-py"
version = "0.1.0"
description = "ClickHouse native protocol client — Python bindings"
requires-python = ">=3.12,<3.15"

[tool.pyrefly]
project-includes = [
    "**/*.py*",
    "**/*.ipynb",
]
